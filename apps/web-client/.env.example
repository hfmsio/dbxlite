# BigQuery Configuration (Optional)
# Leave empty if you don't plan to use BigQuery connector

# Your Google Cloud Project ID
# VITE_BIGQUERY_PROJECT_ID=your-gcp-project-id

# Your BigQuery dataset name
# VITE_BIGQUERY_DATASET=your_dataset_name

# BigQuery location (default: US)
# VITE_BIGQUERY_LOCATION=US

# For local development, BigQuery authentication happens via Google Cloud CLI
# Run: gcloud auth application-default login
# to set up credentials for local testing
