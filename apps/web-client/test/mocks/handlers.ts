/**
 * MSW Request Handlers
 *
 * This file will contain mock request handlers for API testing.
 * Add handlers here as needed for different API endpoints.
 *
 * @example
 * ```ts
 * import { rest } from 'msw'
 *
 * export const handlers = [
 *   rest.get('/api/users', (req, res, ctx) => {
 *     return res(ctx.json([{ id: 1, name: 'John' }]))
 *   }),
 * ]
 * ```
 */

// Placeholder for MSW handlers - will be implemented when MSW is added
export const handlers: never[] = []

// BigQuery mock handlers (to be implemented)
export const bigQueryHandlers: never[] = []

// DuckDB mock handlers (to be implemented)
export const duckDbHandlers: never[] = []
